import React from "react";
import { View } from "react-native";

const SearchUsersList: React.FC = () => {
  return (
    <View
      style={{
        backgroundColor: "#fff",
        width: "100%",
        height: "100%",
        position: "absolute",
        bottom: 0,
      }}
    ></View>
  );
};

export default SearchUsersList;
